type Query {
  meals: [Meal!]!
  meal(id: ID!): Meal!
  categories: [Category!]!
  category(id: ID!): Category!
}

type Category {
  id: ID!
  name: String
  meals: [Meal!]!
}

type Meal {
  id: ID!
  name: String
  price: Float
  quantity: Int
  category: Category
}